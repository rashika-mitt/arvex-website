Add the supplied hero photograph as `public/assets/arvex-workshop-hero.jpg`.
Add the supplied photographs here using these exact filenames:

- `public/assets/products/arvex-workshop.jpg`: the high-resolution workshop image with multiple bats and red balls
- `public/assets/products/arvex-ball-closeup.jpg`: the close-up red cricket ball image
- `public/assets/products/arvex-single-ball.png`: one isolated red cricket ball for the scroll animation

The homepage uses the workshop image for the hero and bat collection, and the close-up image for the floating ball and ball collection. The originals are preserved with CSS cropping only where a section requires it.
The existing SVG files are legacy fallback artwork and are not referenced by the normal product UI. Do not use them for the final product presentation.

Expected files:
- cricket-bat.jpg: front-facing cricket bat photo with visible wood grain and grip texture
- cricket-ball.jpg: red leather cricket ball photo with visible stitching

The current SVG files are fallback slots only. They prevent broken images until the supplied photograph is copied into `public/assets/`.
